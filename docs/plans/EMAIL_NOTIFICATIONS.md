# Plan: notification transports (email, later webhooks)

**Status:** ✅ Complete. Written 2026-08-01 with the maintainer, chunks E1 to E6
shipped 2026-08-01 and 2026-08-02. The per-chunk record, including every
deviation, is the progress table in section 9. Section 8 lists what was
deliberately left out and what builds on this next.

This document is the full implementation brief for a multi-session sub-project.
It is split into **session-sized chunks (E1 to E6)**. Read section 0 (rules), the
orientation in section 1, and then only the chunk you are working on. Every chunk
is independently shippable: after each one the app must be green, migrated and
usable.

---

## 0. Standing rules for every session in this sub-project

These are non-negotiable and apply to E1 through E6.

### 0.1 We ship working code, not a prototype

The maintainer (a single human) manually tests every chunk and will find the bugs
you leave behind. Budget accordingly:

- **Backend:** every new behaviour gets a test in `CheckCheck/backend/tests/`,
  following the existing style (see `tests_sharing_notifications.py`). Run both
  `./run_backend_tests_with_sqlite.sh` and `./run_backend_tests_with_postgres.sh`
  before declaring a chunk done. Postgres is the production target; SQLite is
  local-dev only.
- **Frontend:** unit specs under `frontend/tests/unit/` (vitest) for pure logic,
  Playwright E2E under `frontend/tests/e2e/` for anything a user clicks.
- **Test the failure paths, not just the happy path.** For this feature the
  interesting cases are almost all failures: SMTP down, user has no email address,
  preference says off, throttle window still open, duplicate enqueue, dead-letter
  after N attempts, unsubscribe token expired or forged.
- **Never leave a `TODO` in place of a test.** If a chunk's tests cannot be
  written, the chunk is not done: say so in the handoff instead of claiming
  completion.
- A few DnD and sharing E2E specs fail non-deterministically. Re-run before
  blaming your change.

### 0.2 Every session ends with a cold-start prompt

The last action of every session is to **overwrite `NEXT_SESSION.md`** at the repo
root with a cold-start prompt for the following session, written as **raw
markdown** (the file is a throwaway scratch note and is not committed). It must
contain, at minimum:

- which chunk was just finished and which is next,
- what was actually built versus what this plan specified, including every
  deviation and why,
- anything left broken, skipped or deferred, stated plainly,
- the exact commands to re-run the relevant tests,
- the file paths a fresh session needs to open first.

Also update the **Progress / handoff** table at the bottom of *this* document, so
the durable record lives in git even though `NEXT_SESSION.md` does not.

### 0.3 Writing style

No em dashes or en dashes anywhere in docs, comments, commit messages or UI copy.
Use a comma, colon, parenthesis or a separate sentence.

### 0.4 Schema changes

The 2.0 schema is frozen. Every schema change ships as a real autogenerated
Alembic revision in `CheckCheck/backend/migrations/versions/`, on top of the
current head (`0012_group_shares_living_membership`). Note the create-all-before-
migrations quirk of the test harness: model definitions must be correct for a
fresh Postgres database on their own (naive UTC datetimes, `ondelete="CASCADE"`
on foreign keys).

Watch the `_base_model.py` gotcha: `Field` imported in that module is **pydantic's**
`Field`, so `index` / `primary_key` / `nullable` are ignored there. Use
`from sqlmodel import Field as SQLField` for real DB columns in that file.

### 0.5 Config changes

New settings go into `CheckCheck/backend/checkcheckserver/config.py` with a
`title` and a `description`, following the existing `SHARING_*` block. After
editing, regenerate the docs with `./gen_config_docs.sh` so
`docs/CONFIG_REFERENCE.md` and `config.example.yml` never drift.

If an endpoint or response model changes, regenerate `CheckCheck/openapi.json`
and then the frontend types (`bun run postinstall` in `CheckCheck/frontend`).

---

## 1. Orientation: how notifications work today

### 1.1 The three existing notification types

Defined in
[`model/notification.py:30-36`](../../CheckCheck/backend/checkcheckserver/model/notification.py):

| Type | Meaning | Emitted at | Recipient |
|---|---|---|---|
| `card_shared` | A card was shared with you, instant-share mode | [`api/share_ops.py:125`](../../CheckCheck/backend/checkcheckserver/api/share_ops.py) | the new collaborator |
| `card_invited` | You were invited to a card, invite mode (`SHARING_REQUIRE_INVITE_ACCEPT`) | [`api/share_ops.py:104`](../../CheckCheck/backend/checkcheckserver/api/share_ops.py) | the invitee |
| `public_link_opened` | Someone opened your public link | [`api/access.py:348`](../../CheckCheck/backend/checkcheckserver/api/access.py) | the card owner |

All three carry a `cl_id` and a free-form JSON `payload` whose known keys are
`actor_display_name` and `checklist_name` (see the renderer in
[`components/NotificationBell.vue:139-156`](../../CheckCheck/frontend/components/NotificationBell.vue)).
The payload never holds secrets, and it must stay that way.

Candidate types deliberately **not** added by this plan: `invite_accepted`
(owner learns the invitee accepted), `card_unshared`, `item_mentioned`. A future
`reminder_due` is covered in section 8.

### 1.2 The single emission seam

[`db/notification.py:105`](../../CheckCheck/backend/checkcheckserver/db/notification.py)
`emit_notification()` is the only place a `Notification` row is created. It
persists the row and pushes an SSE nudge (`upd_prop="notification"`) to the
recipient. Its own docstring names it as the hook point for this sub-project.
**Everything in this plan hangs off that one function.** No call site of
`emit_notification` needs to change.

### 1.3 Facts that shape the design

- **`User.email` is `Optional`.** OIDC users can arrive without an email claim.
  `is_email_verified` exists on the model but is set to `False` at registration
  and never flipped anywhere in the codebase. Any send path must handle "no
  address" as an ordinary, non-error outcome.
- **The server runs a single uvicorn process**
  ([`main.py:84-99`](../../CheckCheck/backend/checkcheckserver/main.py)), so no
  cross-process coordination is needed today. Use `FOR UPDATE SKIP LOCKED` on
  Postgres anyway as cheap insurance if someone ever runs replicas.
- **In-process background tasks are already an established pattern here**:
  [`routes_sync_notification.py:353-361`](../../CheckCheck/backend/checkcheckserver/api/routes/routes_sync_notification.py)
  starts `asyncio.create_task` loops from a router lifespan and cancels them on
  shutdown. The email dispatcher follows exactly this shape.
- **`SERVER_PUBLIC_URL`** is the single source of truth for every absolute URL
  the server builds. All email links must come from it. Never reconstruct a URL
  from request headers.
- **Feature gating** reaches the client through `GET /api/public-config`
  ([`routes_public_config.py`](../../CheckCheck/backend/checkcheckserver/api/routes/routes_public_config.py))
  and the `publicConfig` Pinia store.
- **Online-only surfaces**: notification settings are read/written over the
  network only. Follow the WI-12 pattern (`assertOnline` plus `useConnectivity`),
  never queue them in the offline outbox.

---

## 2. Decisions already settled with the maintainer

1. **Preferences are stored as JSON**, one row per user, not as per-type columns.
   Adding a notification type or a channel must not require a migration.
2. **Send path is an outbox table plus one in-process dispatcher loop**, started
   from a lifespan, on the same 30s-tick-plus-nudge pattern as the SSE listener.
   No separate process, no broker, no deployment change. Rationale: FastAPI
   `BackgroundTasks` cannot use `yield` dependencies since FastAPI 0.106 (this
   repo pins `fastapi<0.119`), loses queued mail on any restart, has no retry or
   visibility, and structurally cannot express the three delayed behaviours we
   need (suppress-if-read, coalescing, throttling). The date-reminder feature
   needs the same due-work loop next session regardless.
   **A separate worker process was considered and rejected**, deliberately, not
   by omission. It would buy blast-radius isolation, safe multi-replica scaling
   and protection from blocking code, none of which apply: the server runs a
   single uvicorn process, `aiosmtplib` is async with a timeout, and
   `FOR UPDATE SKIP LOCKED` covers replicas anyway. It would cost the
   single-container deployment story, since every existing user would have to add
   a service to their compose file on upgrade, and it saves no code because the
   outbox table and its locking are needed either way. The decision is cheap to
   reverse later: `drain_once()` is a plain function taking a session, so a
   `notify/worker.py` entrypoint plus a `NOTIFY_DISPATCH_IN_PROCESS: false` flag
   is about 20 lines whenever someone actually needs it. Do not re-open this
   without a concrete trigger (multiple replicas in production, or mail volume
   large enough to matter).
3. **Clicking the card link in an email marks the notification read**, but the
   marking happens in the SPA after the card actually opens, never in a redirect
   endpoint. Mail security scanners pre-fetch links and would otherwise mark
   everything read before the human sees it.
4. **Webhooks are a per-user setting behind an admin master switch** (default
   off), with a private-IP guard. Last chunk, droppable.
5. **"Invite someone to work on this card" by email is in scope** (chunk E6).
   This means mailing a **public link** to an arbitrary address, framed as an
   invitation to collaborate. It works for recipients with **no account at all**,
   because a `CheckListPublicShare` is a capability, not a user grant, and its
   `permission` can be `view`, `check` or `edit`
   ([`model/checklist_public_share.py`](../../CheckCheck/backend/checkcheckserver/model/checklist_public_share.py)).
   An `edit` link therefore lets the recipient genuinely work on the card by
   clicking a link in an email.

   What is **out of scope** is the different thing of provisioning an *account*
   for that person (a pending-invite record keyed on an email address, matched at
   registration), which needs a new identity concept and interacts badly with
   OIDC-only instances where the server does not control signup. See section 8.

---

## 3. Data model

### 3.1 `user_notification_settings` (new table, chunk E3)

One row per user, created lazily on first read or write.

```python
class UserNotificationSettings(TimestampedModel, table=True):
    __tablename__ = "user_notification_settings"
    user_id: uuid.UUID = SQLField(primary_key=True, foreign_key="user.id", ondelete="CASCADE")
    # {"<notification_type>": {"<channel>": "<mode>"}}
    # e.g. {"card_shared": {"email": "immediate"}, "public_link_opened": {"email": "off"}}
    prefs: dict = SQLField(default_factory=dict, sa_column=Column(JSON))
    # IANA name, e.g. "Europe/Berlin". Used for email timestamps today and by the
    # date-reminder feature next session.
    timezone: Optional[str] = SQLField(default=None, max_length=64)
    # Per-user webhook target (chunk E6). Null = no webhook.
    webhook_url: Optional[str] = SQLField(default=None, max_length=2048)
    unsubscribe_secret: str  # random, per user; rotated when the user asks
```

- **Channels**: `in_app`, `email`, `webhook`.
- **Modes**: `off`, `immediate`, `hourly`, `daily`. Not every channel supports
  every mode (`in_app` is `off` or `immediate`; `webhook` likewise). Validate.
- **A missing key means "use the instance default"**, resolved in code. This is
  what makes a new notification type work without a migration and without
  backfilling every user row.
- `in_app` is part of the same matrix so a user can mute the bell too, which is
  free once the resolver exists.

### 3.2 `notification_outbox` (new table, chunk E2)

One row per (notification, channel) delivery attempt.

```python
class NotificationOutbox(TimestampedModel, table=True):
    __tablename__ = "notification_outbox"
    id: uuid.UUID
    user_id: uuid.UUID          # FK user, CASCADE. The recipient.
    notification_id: Optional[uuid.UUID]  # FK notification, CASCADE. Null for non-feed mail (test mail, public-link share).
    channel: str                # "email" | "webhook"
    status: str                 # "pending" | "sent" | "failed" | "cancelled"
    not_before: datetime        # naive UTC. Enables the suppress/coalesce delay.
    attempts: int = 0
    last_error: Optional[str]
    dedupe_key: Optional[str]   # unique-ish key for coalescing, indexed
    payload: dict               # everything the renderer needs, snapshotted at enqueue time
```

Index on `(status, not_before)`, which is the dispatcher's only query.

**Why snapshot the payload** rather than re-reading the notification at send
time: the card may have been renamed, unshared or tombstoned in the meantime, and
re-reading it inside the dispatcher would either leak post-revocation state or
crash. Snapshot at enqueue, when the access check has just passed.

**Retention**: rows in `sent` state older than `NOTIFY_OUTBOX_RETENTION_DAYS`
are pruned by the same loop. `failed` rows are kept longer so an operator can
see what broke.

---

## 4. The dispatcher

New module `checkcheckserver/notify/` with:

- `transports.py`: the channel abstraction. `EmailTransport` protocol with
  `smtp`, `console` (logs the message), `file` (writes `.eml` into a directory)
  and `null` implementations, selected by config. `console`/`file` exist so local
  dev and E2E never need a mail server.
- `render.py`: builds subject plus `text/plain` and `text/html` bodies per
  notification type. Multipart, `List-Unsubscribe` header, stable
  `Message-ID`, and `References` keyed on the card id so a mail client threads
  everything about one card together.
- `prefs.py`: `resolve_mode(user, type, channel) -> Mode`. Order of precedence:
  instance hard-cap (admin disabled the type or channel entirely) beats user
  preference beats instance default beats code default.
- `outbox.py`: `enqueue()`, `drain_once()`, `prune_once()`.
- `dispatcher.py`: the lifespan loop. `while True: await drain_once(); await
  wait_for(nudge_event, timeout=tick)`. Cancelled on shutdown like the SSE tasks.

**`drain_once()` is a plain async function taking a session.** Tests call it
directly and assert on the transport's captured messages. No test ever starts the
loop.

### 4.1 Suppression and coalescing rules

These are the behaviours that make the feature pleasant rather than annoying.
Implement them in `drain_once()`, at the moment a row becomes due:

1. **Already read** wins. If the linked `notification.read_at` is set, mark the
   outbox row `cancelled` and send nothing. This is why `not_before` defaults to
   `now + NOTIFY_EMAIL_SUPPRESS_WINDOW_SECONDS` (default 120) for `immediate`
   mode: a user who is looking at the app right now gets no mail.
2. **Coalescing.** Rows sharing a `dedupe_key` that are due at the same time
   collapse into one message ("Anna shared 5 cards with you"). The key is
   `f"{user_id}:{type}:{actor_id}"` for share events. This is what stops a
   30-person group share turning into 30 separate mails per person.
3. **No special throttle for `public_link_opened`.** It fires **once per link**,
   not on every open: `mark_first_opened` flips `first_opened_at` from null
   atomically and only the very first caller emits
   ([`api/access.py:344-347`](../../CheckCheck/backend/checkcheckserver/api/access.py)).
   So there is no inbox-flood vector here, and its email default is `immediate`
   like the other types. The only residual noise is an owner who deletes and
   recreates links repeatedly, which is owner-driven and already covered by the
   blunt `NOTIFY_EMAIL_MAX_PER_USER_PER_HOUR` cap. Do not add a per-link throttle.

   **Correction recorded 2026-08-01, after chunk E1 was already in progress.**
   The original plan wrongly assumed every open emits, and therefore specified a
   `NOTIFY_PUBLIC_LINK_THROTTLE_MINUTES` setting plus an email default of `off`
   for this type. Section 5 still lists both, deliberately left untouched so the
   running E1 session's spec does not move under it. **Chunk E4 owns the
   cleanup:** delete `NOTIFY_PUBLIC_LINK_THROTTLE_MINUTES` from `config.py` (it
   will have been added by E1 and never read), flip the `public_link_opened`
   email entry in `NOTIFY_DEFAULT_MODES` from `off` to `immediate`, and rerun
   `./gen_config_docs.sh`. Neither has any effect before E4, since nothing reads
   the defaults until the fan-out exists.
4. **No address, no send.** A user with `email is None` produces no outbox row at
   all (do not enqueue and then fail). Log at debug, not warning.
5. **Verification.** `NOTIFY_EMAIL_REQUIRE_VERIFIED` defaults to `false`, because
   in a self-hosted instance addresses come from OIDC or an admin and are
   trusted. When true, an unverified address is skipped.

### 4.2 Retry policy

Exponential backoff on transient failures (connection refused, 4xx SMTP), capped
at `NOTIFY_MAX_ATTEMPTS` (default 6), after which the row goes `failed` and stays
for inspection. Permanent failures (5xx SMTP, invalid address) go straight to
`failed` without retrying. Never retry inside a request.

---

## 5. Configuration (chunk E1)

New `config.py` block, all with `title` and `description`, regenerate docs after.

| Setting | Default | Purpose |
|---|---|---|
| `EMAIL_ENABLED` | `false` | Master switch. Off means no mail is ever sent and the settings UI hides the email column. |
| `EMAIL_TRANSPORT` | `smtp` | `smtp` / `console` / `file` / `null`. |
| `EMAIL_SMTP_HOST` | `None` | |
| `EMAIL_SMTP_PORT` | `587` | |
| `EMAIL_SMTP_USER` | `None` | |
| `EMAIL_SMTP_PASSWORD` | `None` (SecretStr) | |
| `EMAIL_SMTP_SECURITY` | `starttls` | `starttls` / `ssl` / `none`. |
| `EMAIL_FROM_ADDRESS` | `None` | Required when `EMAIL_ENABLED`. Validate at startup. |
| `EMAIL_FROM_NAME` | `APP_NAME` | |
| `EMAIL_REPLY_TO` | `None` | |
| `EMAIL_FILE_TRANSPORT_DIR` | `./dev_mail` | Where the `file` transport writes `.eml`. |
| `EMAIL_TIMEOUT_SECONDS` | `20` | |
| `NOTIFY_EMAIL_REQUIRE_VERIFIED` | `false` | See 4.1.5. |
| `NOTIFY_EMAIL_CONTENT_MODE` | `full` | `full` includes card and actor names, `minimal` says only that something happened plus a link. Mail leaves the instance, so operators need this choice. |
| `NOTIFY_EMAIL_SUPPRESS_WINDOW_SECONDS` | `120` | See 4.1.1. |
| `NOTIFY_PUBLIC_LINK_THROTTLE_MINUTES` | `60` | See 4.1.3. |
| `NOTIFY_DEFAULT_MODES` | see below | Instance defaults per type and channel. |
| `NOTIFY_DISABLED_TYPES` | `[]` | Admin hard-cap. A user cannot opt into a type listed here. |
| `NOTIFY_DISPATCH_TICK_SECONDS` | `30` | |
| `NOTIFY_MAX_ATTEMPTS` | `6` | |
| `NOTIFY_OUTBOX_RETENTION_DAYS` | `30` | |
| `NOTIFY_FEED_RETENTION_DAYS` | `180` | Prunes the `notification` table itself, which grows forever today. `0` disables pruning. |
| `NOTIFY_EMAIL_MAX_PER_USER_PER_HOUR` | `20` | Blunt anti-flood backstop. |
| `NOTIFY_WEBHOOK_ENABLED` | `false` | Chunk E6. Master switch for per-user webhooks. |
| `NOTIFY_WEBHOOK_ALLOW_PRIVATE_IPS` | `false` | Chunk E6. SSRF guard. |
| `SHARING_PUBLIC_LINK_EMAIL_ENABLED` | `false` | Chunk E6. Lets a user mail a public link to an arbitrary address. |
| `SHARING_PUBLIC_LINK_EMAIL_MAX_PER_HOUR` | `10` | Chunk E6, added there. Per-sender limit; the thing that keeps the setting above from being a mail relay. |
| `SHARING_INTERNAL_EMAIL_DOMAINS` | `[]` | Chunk E6, added there. Domains the "that address looks like a colleague's" hint fires on. |

Suggested `NOTIFY_DEFAULT_MODES`:

```yaml
card_shared:        { in_app: immediate, email: immediate }
card_invited:       { in_app: immediate, email: immediate }
public_link_opened: { in_app: immediate, email: off }
```

`GET /api/public-config` gains `email_enabled` and `webhook_enabled` so the
frontend hides the whole settings section on an instance without mail configured.

---

## 6. API surface

| Method | Path | Chunk | Notes |
|---|---|---|---|
| `GET` | `/api/user/me/notification-settings` | E3 | Returns the resolved matrix (effective mode per type per channel) plus which entries are admin-locked, plus timezone. The UI must be able to show "your admin disabled this". |
| `PUT` | `/api/user/me/notification-settings` | E3 | Partial update of the matrix, timezone, webhook url. Rejects a mode the admin has capped, with a clear 4xx. |
| `POST` | `/api/user/me/notification-settings/test-email` | E2/E5 | Sends a test mail to the current user. Rate-limited hard (one per minute). 409 when the user has no address or mail is disabled. |
| `GET` | `/api/notifications/unsubscribe` | E4 | Public, no session. Takes a signed token (`user_id`, `type`, expiry, HMAC over `unsubscribe_secret`). Shows a tiny confirm page, then sets that type's email mode to `off`. One-click, per RFC 8058 expectations. Forged or expired tokens get a neutral error page, never a stack trace and never a hint about whether the user exists. |
| `POST` | `/api/checklist/{id}/public-share/email` | E6 | "Invite someone to work on this card": mails an existing public link to a given address, with an optional personal message. The recipient needs no account and gets whatever the link grants (`view`, `check` or `edit`). Owner only, requires the public-links feature plus `SHARING_PUBLIC_LINK_EMAIL_ENABLED`, rate-limited per user per hour. |
| `GET` | `/api/sharing/public-link-email-options` | E6 | What the client needs to render that field: whether the instance accepts it, the message-length and hourly limits, and `SHARING_INTERNAL_EMAIL_DOMAINS`. Authenticated, unlike `/public-config`: the domain list is the operator's information about their organisation. |
| `POST` | `/api/user/me/notification-settings/test-webhook` | E6 | The webhook twin of the test mail: queues one POST to the caller's own URL. 409 without a URL or with webhooks off, 429 once a minute. A URL the SSRF guard refuses still gets a 202 and fails in the queue, since the guard runs at delivery time. |

---

## 7. Session chunks

Each chunk is one session. Do not start the next one in the same session even if
there is time left: finish, test, write the handoff.

### E1: config, transports, and the mail test harness

**Goal:** the server can send an email, and tests can observe it, but nothing
sends one yet.

- Full config block from section 5 (webhook and public-link-email settings can
  come later, but adding them now avoids a second `gen_config_docs.sh` pass).
- Startup validation: `EMAIL_ENABLED` with no `EMAIL_FROM_ADDRESS` or, for the
  `smtp` transport, no `EMAIL_SMTP_HOST`, must fail loudly at boot rather than
  silently swallowing mail at runtime.
- `notify/transports.py` with all four transports. Add `aiosmtplib` to
  `pyproject.toml` and sync **both** venvs (pdm-managed `backend/.venv` for the
  dev server, uv root `.venv` for pytest).
- `email_enabled` on `GET /api/public-config`.
- Test harness: a pytest fixture giving a capturing transport, plus one real
  end-to-end SMTP test against an in-process `aiosmtpd` server so the SMTP code
  path is genuinely exercised and not just mocked.

**Tests:** transport selection per config, startup validation failures, multipart
structure, headers, `aiosmtpd` round trip, `console` and `file` transports.

### E2: outbox table and dispatcher loop

**Goal:** durable queued delivery with retry, driven by a lifespan task.

- Alembic revision `0013` for `notification_outbox`.
- `notify/outbox.py`: `enqueue`, `drain_once`, `prune_once`, with `FOR UPDATE
  SKIP LOCKED` on Postgres.
- `notify/dispatcher.py`: the lifespan loop plus the nudge event, wired the same
  way as the SSE listener, cancelled cleanly on shutdown.
- `POST /api/user/me/notification-settings/test-email` as the first real
  producer, so the whole path is exercisable by hand from day one.
- Retry, backoff, dead-letter, retention pruning.

**Tests:** enqueue then drain sends once; transient failure retries with growing
backoff; `NOTIFY_MAX_ATTEMPTS` reaches `failed`; permanent failure does not
retry; `not_before` in the future is not picked up; concurrent drains do not
double-send; pruning removes the right rows and keeps `failed`; loop starts and
cancels cleanly.

### E3: preference model, resolution, and API

**Goal:** users have preferences; nothing consumes them yet.

- Alembic revision `0014` for `user_notification_settings`, lazily created rows,
  per-user `unsubscribe_secret`.
- `notify/prefs.py` resolver with the precedence from section 3.1, plus
  validation of which modes each channel accepts.
- `GET` and `PUT /api/user/me/notification-settings`, returning effective modes
  and admin-locked flags.
- Timezone field, validated against the IANA database.

**Tests:** the full resolution matrix (unset key falls back to instance default,
user override wins, admin cap beats user override); `PUT` rejects a capped mode;
`PUT` rejects an unknown type, channel, mode or timezone; a brand new user with
no row gets sane defaults; a new notification type added in code needs no
migration and no backfill.

### E4: wiring, rendering, suppression, unsubscribe

**Goal:** the three real notification types actually send mail, correctly.

- `emit_notification()` fans out to `enqueue()` for every channel whose resolved
  mode is not `off`. **This is the only change to existing call sites, and there
  should be none.**
- `notify/render.py` templates for all three types, in both `full` and `minimal`
  content modes, text and HTML.
- Deep links built from `SERVER_PUBLIC_URL` as `/?card={cl_id}&n={notification_id}`.
- Suppression, coalescing and throttling from section 4.1.
- Hourly and daily digest assembly.
- Unsubscribe endpoint plus `List-Unsubscribe` header.
- Per-user hourly send cap.

**Tests:** one test per type end to end through the capturing transport; no mail
when the mode is `off`; no mail and no row when the user has no address; mail
suppressed when the notification was read inside the window; a group share to N
users produces N mails, not N squared; five shares from one actor coalesce into
one mail; `public_link_opened` respects the throttle; `minimal` mode leaks
neither card name nor actor name; unsubscribe with a valid token flips exactly
one type to `off`; forged, tampered and expired tokens are rejected; digests
collect the right window and empty windows send nothing.

### E5: frontend settings UI and read-on-open

**Goal:** the user can see and change all of this, and email links behave.

- `NotificationSettingsModal.vue`, opened from the user menu in
  [`Navbar.vue:110-130`](../../CheckCheck/frontend/components/Navbar.vue) next to
  the existing "API keys" entry, following the `ApiKeysModal` pattern
  (declarative `v-model:open` so it cannot double-dialog).
- A per-type row with a channel column each for in-app and email, mode select,
  admin-locked entries rendered disabled with a reason, timezone picker, and a
  "Send test email" button with success and failure feedback.
- Hidden entirely when `email_enabled` is false; the in-app column still shows.
- Online-only: `assertOnline` plus a `useConnectivity` notice, never queued in
  the offline outbox.
- Read-on-open: `pages/index.vue` reads the `n` query parameter and calls
  `notificationStore.markRead(n)` **after** the card overlay has rendered, then
  strips the parameter from the URL.

**Tests:** unit specs for the resolution display logic and the query-parameter
handler; E2E for opening the modal, changing a mode and seeing it persist across
reload, the disabled state when the admin capped a type, the offline notice, and
the `?card=&n=` deep link marking exactly that notification read.

### E6: invite-by-email for public cards, webhooks, feed retention

**Goal:** the user-facing extra the maintainer asked for, plus two smaller items.
Order matters here: the card invitation is the valuable part, webhooks are
droppable if the session runs long.

1. **"Invite someone to work on this card" (do this first).**
   `POST /api/checklist/{id}/public-share/email`. Mails an existing public link
   to an address with an optional personal message, framed as an invitation, not
   as a notification. The recipient needs no account: the link is a capability
   and can grant `view`, `check` or `edit`, so an `edit` link means they can
   really work on the card straight from the email.
   - Owner only. Requires `SHARING_ENABLED`, `SHARING_PUBLIC_LINKS_ENABLED` and
     `SHARING_PUBLIC_LINK_EMAIL_ENABLED`.
   - The link must already exist. This endpoint never creates or upgrades one,
     so a user cannot accidentally mail out more access than they meant to.
   - **This is an open mail relay if you get it wrong.** Any authenticated user
     can name an arbitrary recipient. Rate-limit per sender per hour, cap the
     personal message length, and never echo the address back in an error
     response (no enumeration, no reflected content).
   - If the link carries a passphrase, the mail must **not** include it. Say that
     a passphrase is required and let the sender share it out of band.
   - **Frontend, and this part is not optional polish.** The single biggest risk
     with this feature is the Nextcloud failure mode the maintainer has watched
     users hit repeatedly: someone wants to share with a *colleague who has an
     account*, thinks "I know their email", and uses the external-link email
     field instead of the collaborator box. They then hand out an anonymous
     capability link when they meant a per-user grant. Design against it:

     1. **Placement is the primary defence.** The field lives *inside* the
        public-link block and only renders once a public link exists. It is
        never a sibling of the "Add people" collaborator box. Because the
        endpoint requires an existing link, the field cannot be a user's first
        move, which is precisely what makes the confusion possible in Nextcloud.
     2. **Name it by audience, not mechanism.** Not "Invite by email". Use
        "Send this link to someone without an account", under a heading like
        "People outside {APP_NAME}".
     3. **Spell out the consequence difference** next to it, one line each,
        because this is the thing users actually get wrong:
        collaborator = the card appears in their board, tied to their account,
        revoke them individually; link by email = anyone holding the link gets
        in without signing in, and revoking kills it for everyone.
     4. **Soft "that looks internal" hint** driven by a new
        `SHARING_INTERNAL_EMAIL_DOMAINS` config list (default empty). When the
        typed address is on a declared domain, show a callout: "That address
        looks internal. If they have an account, add them as a collaborator
        above instead", with "Add as collaborator" as the primary action and
        "Send link anyway" as the secondary one.
     5. **Never hard-block.** Sending to a member's address is sometimes exactly
        right: a personal address, or a device where they are not signed in.
     6. **Confirm step** states the granted level in plain words: "They will be
        able to edit this card without signing in."

     **Do not implement the hint as a live per-address membership lookup.**
     That was considered and rejected: it hands every account holder an oracle
     for "does this address have an account here", which is exactly what
     [`db/user.py:107`](../../CheckCheck/backend/checkcheckserver/db/user.py)
     deliberately avoids ("Deliberately does NOT match on email to avoid address
     enumeration"), and the user-search response model never returns email
     either. The domain list in point 4 catches the real-world case (a colleague
     on the company domain) without disclosing anything the operator did not
     declare. If an instance owner wants the exact per-address behaviour anyway,
     it is a legitimate choice for a trusted self-hosted deployment, but it ships
     as `SHARING_EMAIL_MEMBER_HINT` defaulting to **off**, requiring
     `SHARING_USER_SEARCH_ENABLED`, rate-limited, and documented as "reveals to
     your users whether an email address has an account on this server".
   - Online-only surface, like the rest of the share UI.
2. **Webhook channel.** Per-user `webhook_url`, admin master switch, generic JSON
   payload, SSRF guard rejecting private and loopback ranges unless
   `NOTIFY_WEBHOOK_ALLOW_PRIVATE_IPS`, short timeout, same retry policy, plus a
   "send test webhook" action.
3. **Feed retention** pruning of the `notification` table via
   `NOTIFY_FEED_RETENTION_DAYS`, in the same loop.
4. **Config work owned by this chunk.** `SHARING_INTERNAL_EMAIL_DOMAINS` (and
   `SHARING_EMAIL_MEMBER_HINT` if the operator-choice variant is wanted) are
   **not** in the section 5 table and are **not** E1's job: they were decided
   after E1 was already in progress, so add them here and rerun
   `./gen_config_docs.sh`. While you are in there, confirm the E4 cleanup landed:
   `NOTIFY_PUBLIC_LINK_THROTTLE_MINUTES` should be gone (see 4.1.3).

**Tests:** invitation mail requires ownership and all three flags; a
non-owner collaborator gets 403; the rate limit blocks the second send in the
window; the passphrase never appears in the rendered mail; an `edit` link in a
test mail actually resolves to an editable card; the address is absent from every
error body. E2E: the email field is not visible until a public link exists; an
address on a `SHARING_INTERNAL_EMAIL_DOMAINS` domain raises the callout and its
primary action lands in the collaborator box with the address carried over; "send
link anyway" still works; with an empty domain list no callout ever appears. Webhook posts the expected body and honours the master switch;
`http://169.254.169.254` and `http://127.0.0.1` are refused by default and
allowed when the flag is on; hostnames resolving to private IPs are refused too.
Feed pruning keeps unread rows inside the window.

---

## 8. Explicitly out of scope, and what comes next

- **Date-based reminders** ("notify me about that checklist at date"). **Now
  planned in its own document, [`DATE_REMINDERS.md`](DATE_REMINDERS.md)**
  (written 2026-08-02, chunks R1 to R4). It reuses this infrastructure directly:
  a `scheduled_notification` table whose due rows are picked up by the same
  dispatcher loop, emitting a new `reminder_due` notification type through
  `emit_notification`. The per-user `timezone` field in section 3.1 exists
  specifically so that feature does not need another migration. The two open
  questions this section listed are answered there as decisions 1 and 2: only
  the user who set a reminder receives it, and validity is re-checked at fire
  time with a silent cancel rather than eagerly at each revocation seam.
- **Provisioning an account for someone who does not have one.** Note the
  distinction from chunk E6: inviting a stranger to *work on a card* is in
  scope and needs no account, because a public link is a capability. What is out
  of scope is creating a *user* from an email invitation, which needs a pending
  invite record keyed on an address, matched at registration, and conflicts with
  OIDC-only instances where the server does not control signup. If a recipient of
  a card invitation later wants a real account with the card in their own board,
  that is the normal signup plus share flow, not this feature.
- **Email address verification and change flow.** `is_email_verified` stays dead
  code for now. The plumbing built here (transports, outbox, signed tokens) is
  most of what it would need.
- **Web push.** The channel abstraction accommodates it, but it needs VAPID keys
  and service-worker work that has nothing to do with this plan.
- **Per-card mute** ("stop notifying me about this card"). Worth doing, but it
  belongs with the reminder feature, where it becomes genuinely necessary.

---

## 9. Progress / handoff

Update this table at the end of every session. Keep it honest: partial means
partial.

| Chunk | Status | Session date | Notes and deviations |
|---|---|---|---|
| E1 config, transports, harness | ✅ Done | 2026-08-01 | Full section 5 config block including the E6 settings, boot-time validation as a `Config` model validator, `notify/transports.py` with all four transports plus a `CapturingEmailTransport` test seam, `aiosmtplib` runtime dep and `aiosmtpd` test dep (both venvs synced), `email_enabled` **and** `webhook_enabled` on `/api/public-config`, 26 tests in `tests_email_transports.py` including a real aiosmtpd round trip. Deviations, all small: (a) `webhook_enabled` was added to public-config now rather than in E6, same reasoning as adding the E6 config settings early (one openapi regeneration instead of two); (b) `build_email_transport()` returns the null transport whenever `EMAIL_ENABLED` is false, so the master switch cannot be defeated by a caller that forgets to check it; (c) transports classify failures into `TransientEmailError` / `PermanentEmailError` (SMTP 4xx vs 5xx), which E2's retry policy consumes; (d) `EMAIL_FROM_NAME` is resolved to `APP_NAME` in the validator rather than at use time. |
| E2 outbox and dispatcher | ✅ Done | 2026-08-01 | `notification_outbox` table plus Alembic revision `0013` (verified on SQLite **and** Postgres to build exactly what `create_all` builds, and to be a no-op when `create_all` got there first), `notify/outbox.py` (`enqueue` / `drain_once` / `prune_once` / `count_recent`), `notify/dispatcher.py` (lifespan task plus nudge, attached to the new `routes_notification_settings.py` router), and `POST /api/user/me/notification-settings/test-email` as the first real producer (202, 409 without an address or with mail off, 429 once a minute). 16 tests in `tests_notification_outbox.py`, green on both backends, plus a real-boot smoke test with the `file` transport (endpoint to `.eml` on disk in one tick). Deviations: (a) **one new setting**, `NOTIFY_DISPATCH_IN_PROCESS` (default true), the flag decision 2 already described as the reversal seam; it also makes the tests deterministic, since the test server must not drain the queue behind them; (b) the loop does not start at all when no channel can deliver (`EMAIL_ENABLED` and `NOTIFY_WEBHOOK_ENABLED` both false), because nothing can reach the outbox then; (c) a row is claimed by a conditional `UPDATE ... WHERE status = 'pending' AND attempts = n` rather than a `sending` status, which is what actually prevents a double send (SQLite has no `SKIP LOCKED`) and leaves no row stuck if the process dies mid-send; the claim also schedules the retry up front; (d) suppression rule 4.1.1 (cancel when the notification was already read) is implemented here rather than in E4, it is six lines and belongs next to the delivery decision; coalescing, throttling and digests stay in E4; (e) `prune_once` also removes old `cancelled` rows, not only `sent`; (f) the test harness now rebinds the app's shared async engine after the database is chosen (`conftest._rebind_engine_in_test_process`), because a test module imports the engine during collection and in-process database work would otherwise land in SQLite during a `--db=postgres` run. |
| E3 preferences | ✅ Done | 2026-08-01 | `user_notification_settings` table plus Alembic revision `0014` (verified on SQLite **and** Postgres to build exactly what `create_all` builds, to be a no-op when `create_all` got there first, and to survive a downgrade plus re-upgrade), `notify/prefs.py` (the `resolve_mode` precedence, the per-channel mode rules, patch validation and merging, IANA time-zone validation), `db/user_notification_settings.py` (lazy row creation, race-tolerant), and `GET` / `PUT /api/user/me/notification-settings` returning the effective matrix with per-entry `user_choice`, `default_mode`, `locked` and `locked_reason`. 20 tests in `tests_notification_prefs.py`, green on both backends, plus a real-boot smoke test on a database sitting at `0013` (the upgrade ran, the endpoints answered, 400 for a bad time zone and 409 for a webhook on an instance without webhooks). No new config setting. Deviations: (a) **the row is created on write only**, not on read: `GET` returns the instance defaults for a user who has no row, since a read endpoint that writes is a surprise in the logs and the plan allowed either; (b) `resolve_mode` takes the **settings row**, not a `User`, because the `User` row carries no preferences and the caller has to load this one anyway, which also keeps the resolver free of database access; (c) the hard cap covers **channels as well as types**: a channel whose master switch is off (`EMAIL_ENABLED`, `NOTIFY_WEBHOOK_ENABLED`) is locked off exactly like a type in `NOTIFY_DISABLED_TYPES`, which is what the settings dialog needs to explain itself; (d) an unusable stored mode (garbage in `NOTIFY_DEFAULT_MODES`, or a mode a channel cannot honour) is logged and **skipped** at resolution time rather than raised on, so one bad YAML entry cannot take down every notification, while the write path rejects the same input loudly; (e) the code default for an unconfigured type is `in_app: immediate`, `email: off`, `webhook: off`, so a notification type added in a later release never starts mailing on its own; (f) two status codes rather than one, 400 for something that does not exist and 409 for something an administrator locked; (g) `webhook_url` is stored and shape-checked here, but the SSRF guard stays E6's, because a hostname's address can change between validation and use. |
| E4 wiring and rendering | ✅ Done | 2026-08-02 | The fan-out lives in `emit_notification` and no call site changed. New modules: `notify/render.py` (one renderer for a single message, a coalesced one and a digest, `full` and `minimal` content modes, text plus HTML, `List-Unsubscribe` / `List-Unsubscribe-Post` / `References` / `In-Reply-To`, deep links `/?card=&n=` from `SERVER_PUBLIC_URL` only), `notify/schedule.py` (suppression window, digest windows, dedupe keys) and `notify/unsubscribe.py` (HMAC-SHA256 token over the user's own `unsubscribe_secret`, 90 day TTL). `outbox.drain_once` grew coalescing (a claimed row also claims every due row sharing its `dedupe_key`) and `count_recent_for_user`; `GET` and `POST /api/notifications/unsubscribe` are public. Config cleanup from 4.1.3 done: `NOTIFY_PUBLIC_LINK_THROTTLE_MINUTES` deleted, `public_link_opened` email default flipped to `immediate`, `./gen_config_docs.sh` rerun, `docs/configuration.md` extended, openapi and frontend types regenerated. 23 tests in `tests_notification_wiring.py`, green on both backends. Deviations: (a) **`in_app: off` suppresses the `Notification` row itself** (the open question this chunk had to answer): a row that exists is in the bell, the badge and the delta feed however it is flagged, so the only honest way to honour the preference is not to write it, and `emit_notification` now returns `Optional[Notification]`; (b) **a digest is not assembled by anything**, it is coalescing with the window's end as both the due time and the key, which is why an empty window costs nothing and a digest with one item is simply that item's own message; (c) the daily digest hour is a module constant (`schedule.DAILY_DIGEST_HOUR`, 08:00 local), **not a new config setting**, since the per-user time zone already personalises it; (d) **unsubscribe is split**: `GET` renders a confirmation page and changes nothing (a mail scanner pre-fetching links must not unsubscribe people), `POST` acts and is also the RFC 8058 one-click target, and it writes the preference directly rather than through `validate_prefs_patch` so switching a channel *off* works whatever an administrator locked; (e) the hourly cap is applied **at enqueue time**, counting queued rows across every dedupe key, because queueing the overflow would only deliver the flood later; (f) `emit_notification` never raises at its caller: a message that cannot be queued is logged and the session rolled back, since a share must not fail over mail, and it gained an optional `config` parameter as a test seam; (g) **no webhook rows are enqueued at all**, E6 owns that channel, so a row for it cannot sit in the queue being dead-lettered; (h) only rows carrying a render context coalesce, which keeps a stand-alone message (the test mail, whose dedupe key exists for rate limiting) out of somebody's digest; (i) the plan's "public_link_opened respects the throttle" test became the per-user hourly cap test, following the 4.1.3 correction that removed the throttle. One existing assertion in `tests_notification_outbox.py` now filters captured mail by subject as well as recipient: from this chunk on, every share in the suite queues real mail. One unrelated test-harness fix was needed too: `tests_sharing_sync._SSECollector` used a 3 second read timeout, and a read timeout there ends collection for good rather than just unblocking the read, so a collector opened two seconds before the event it waits for (the owner's stream in `test_bulk_group_revoke_emits_single_share_removed_broadcast`) died just as the event arrived; the extra write load this chunk puts on SQLite made that latent race fire in most runs. Raised to 12 seconds. The four sibling copies of that collector class still carry the 3 second value and the same latent problem. |
| E5 frontend UI | ✅ Done | 2026-08-02 | `components/NotificationSettingsModal.vue`, opened from a new "Notifications" entry in the avatar menu next to "API keys", built on the same declarative `v-model:open` pattern. One block per notification type with a mode select per channel (in-app and email), locked entries disabled with the server's `locked_reason` beside them, a time-zone picker and a "Send test email" button with per-status feedback (202 queued, 409 no address or mail off, 429 once a minute). The display logic is a framework-free module, `utils/notificationSettings.ts`, and the dialog is a thin template over it. Read-on-open is `utils/notificationDeepLink.ts` plus `composables/useNotificationDeepLink.ts`, wired in `pages/index.vue`. Store: `settings` plus `fetchSettings` / `saveSettings` / `sendTestEmail` on the existing notification store, all behind `assertOnline`. 32 vitest cases in `tests/unit/notificationSettings.spec.ts` and `tests/unit/notificationDeepLink.spec.ts`, 5 Playwright cases in `tests/e2e/notification-settings.spec.ts`. Deviations: (a) **the mail link's `?card=` is translated by the client**, not just `?n=`: the app's card route is the path `/card/<id>`, so the handler rewrites the query link to it and only then handles `n`, which is what makes the deep link open anything at all; (b) **there is no Save button**: every change is its own partial `PUT` naming exactly one cell, and the response (the full effective matrix) replaces what the dialog holds, so a capped or rejected entry self-corrects instead of leaving a stale draft on screen; (c) inheriting is rendered as an explicit "Default (…)" entry in the select, and picking it sends `null`, which is the only way to tell it apart from choosing today's default by hand; (d) a locked cell shows the *effective* mode (`mode`), not the override underneath it, since that override is not what the server is doing right now; (e) **the menu entry follows `sharing_enabled`**, like the bell itself: nothing emits notifications on an instance with sharing off; (f) the entry is deliberately **not** disabled offline, unlike Logout, because the dialog opening and explaining itself beats a dead menu item; (g) the deep-link handler also calls `refreshUnread()` after marking read, since the feed is usually not loaded when a link is followed from an inbox and the store's optimistic decrement would have nothing to decrement; (h) it gives up waiting for the card after 5 seconds and marks the notification read anyway, so a deleted or revoked card cannot leave it unread forever; (i) it re-reads the URL before stripping `n`, so a user who navigated away during the wait is not dragged back; (j) no webhook column (E6 owns it), and no `is_email_verified` surface anywhere. Test-harness change: `backend/e2e/start_e2e_server.py` now boots mail-capable (`EMAIL_ENABLED` with the `null` transport, which queues and discards) and sets `NOTIFY_DISABLED_TYPES: ["public_link_opened"]`, so the E2E instance can exercise the email half of the dialog and has one genuinely locked entry; both are `setdefault`, so a caller can still override them. One unrelated pre-existing failure fixed while here: `tests/e2e/auth.spec.ts` waited for an exact `/login` after logout, which stopped matching when the OIDC work started appending `?logout=1`; verified failing on a clean tree before the fix. |
| E6 invitations, webhooks, retention | ✅ Done | 2026-08-02 | **1. Mailing a public link.** `POST /api/checklist/{id}/public-share/email` next to the other public-link endpoints, owner only, behind `SHARING_ENABLED` + `SHARING_PUBLIC_LINKS_ENABLED` + `SHARING_PUBLIC_LINK_EMAIL_ENABLED` (404) and `EMAIL_ENABLED` (409). It never creates, enables or upgrades a link, refuses a disabled or expired one, caps the personal note, rate-limits per sender against the outbox, and repeats the recipient's address in **no** response, error or success. `notify/invitation.py` renders it (no unsubscribe, no render context so it can never join a digest, and the link's passphrase is announced but never included). **2. The webhook channel.** `notify/webhooks.py` (SSRF guard plus the httpx sender), a `webhook` branch in `outbox.drain_once`, `_queue_webhook` in the fan-out, `render.webhook_body`, and `POST /api/user/me/notification-settings/test-webhook`. **3. Feed retention.** `db.notification.prune_feed_once`, run next to `prune_once` in the dispatcher's hourly housekeeping. **4. Config.** `SHARING_INTERNAL_EMAIL_DOMAINS` and `SHARING_PUBLIC_LINK_EMAIL_MAX_PER_HOUR` added, `gen_config_docs.sh` rerun, `docs/configuration.md` extended with three new sections; the E4 cleanup was confirmed (`NOTIFY_PUBLIC_LINK_THROTTLE_MINUTES` is gone). **Frontend.** The invitation field lives inside `ShareModal/PublicLinks.vue` and only once a link exists, with audience wording, the two consequence lines side by side, the soft internal-domain callout and a confirm step that states the granted level; `utils/publicLinkEmail.ts` holds all of it as pure functions. The webhook channel is a third column in the settings dialog plus a URL field and a test button (`visibleChannels` in `utils/notificationSettings.ts`). 46 backend tests in `tests_public_link_email.py` and `tests_notification_webhooks.py`, 27 new vitest cases, 6 new Playwright cases plus one for the webhook UI. Deviations: (a) **two new settings, not one**: a per-sender hourly limit needs a number, and hard-coding the one control that stops this being an open relay was not defensible, so `SHARING_PUBLIC_LINK_EMAIL_MAX_PER_HOUR` exists; `SHARING_EMAIL_MEMBER_HINT` was **not** built, since the plan made it optional and the domain list covers the real case without handing anybody an address oracle. (b) **The domain list is served from a new authenticated endpoint**, `GET /api/sharing/public-link-email-options`, not from `/public-config`: the boolean is a feature flag and belongs on the unauthenticated bootstrap, but a list of an operator's own domains is data and does not. (c) **The hint's primary action carries the address's local part, not the address**, because the user search never matches on email (deliberately, `db/user.py:107`), so the full address would put a term in the box that can never match; `AddPeople.vue` exposes `startSearch()` for it. (d) **The confirm step is inline, not a nested dialog**: a modal inside a modal is the exact shape of the double-dialog trouble this codebase already fixed once. (e) **`NOTIFY_EMAIL_CONTENT_MODE` is honoured by the invitation and by the webhook body** despite its name: both leave the instance, and the invitation goes to an address somebody typed, so a typo sends it to a stranger. (f) **The webhook body is not signed**; it carries nothing secret, and a receiver that needs authenticity can put a token in the URL (which is never logged). (g) **The SSRF guard pins the request to the address it checked** (original `Host`, original SNI) and does not follow redirects, because a name that answers "public" to the check and "127.0.0.1" to the connection would otherwise walk straight past it. (h) **Webhook rows never coalesce and are never read-suppressed**: merging is a kindness to a human inbox, and an integration is expected to fire whatever the human did. (i) **Feed retention prunes read rows only**, which is what the setting's own description says and what makes it safe to run unattended: an unread notification is still somebody's inbox and its badge is the only sign the event happened. The consequence is that an account which never opens the bell still accumulates rows. (j) **The dispatcher now starts for retention alone**: `dispatch_enabled` is true when feed retention is on even with no channel, while `dispatch_once` skips the drain, so an instance with neither mail nor webhooks tidies its feed without polling a table nothing can write to. Two pre-existing problems fixed while here: the dispatcher's `last_prune = 0.0` compared against `time.monotonic()`, which counts from boot, so whether housekeeping ran on the first pass depended on the *host's uptime* (it is `None` now, meaning "never yet"); and `tests_sharing_prereqs.py` gained the new public-config key. |
| Dev harness (local sink) | ✅ Done | 2026-08-02 | Not a planned chunk. Both `run_dev_backend_server_with_oidc*.sh` gained `--mail=mailpit|file|console|off` (default: mailpit when docker is usable, file otherwise), which enables mail, boots a `checkcheck-dev-mailpit` container (SMTP 1025, inbox 8025, removed by the Postgres script's `--reset`), and shortens `NOTIFY_EMAIL_SUPPRESS_WINDOW_SECONDS` / `NOTIFY_DISPATCH_TICK_SECONDS` to 5s plus turns on the two off-by-default surfaces (`SHARING_PUBLIC_LINK_EMAIL_ENABLED`, `NOTIFY_WEBHOOK_ENABLED` with private IPs). Every value is a `${VAR:-default}` so a caller can override it. Documented in `docs/development.md` (trigger list, and the pointer to `notification_outbox` for anything that does not arrive). Verified end to end: the real `smtp` transport delivered into Mailpit and the message showed up over its API. |
